apiVersion: v1
kind: Service

metadata:
  name: nginx

spec:
  selectors:
    project: roboshop
    component: frontend
    environment: dev

  ports:
  - protocol: "tcp"
    port: 80 # service port
    targetPort: 80 # target port